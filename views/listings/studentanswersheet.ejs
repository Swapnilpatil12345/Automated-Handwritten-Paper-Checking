<% layout('/layouts/boilerplate') %>
<body>
    <div class="bdy">
        <h3 class="studentanswersheet-h3">Please upload the Student's Exam Answersheet</h3>
        <form action="/papers/<%=listing._id%>/studentanswersheet/" enctype="multipart/form-data" method="post" class="studentanswersheetform">
            <label for="listing[StudentAnswerSheet]" class="custom-file-upload">
                Upload File
            </label>
            <input id="listing[StudentAnswerSheet]" name="listing[StudentAnswerSheet]" type="file" required onchange="updateFileName()">
            <span id="file-name" class="file-name">No file chosen</span>
            <button class="btn btn-dark btn-submit" type="submit">Submit</button>
        </form>
    </div>

    <script>
        function updateFileName() {
            const fileInput = document.getElementById('listing[StudentAnswerSheet]');
            const fileNameDisplay = document.getElementById('file-name');
            fileNameDisplay.textContent = fileInput.files.length > 0 ? fileInput.files[0].name : 'No file chosen';
        }
    </script>
</body>